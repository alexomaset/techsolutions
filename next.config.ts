import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      'source.unsplash.com',
      'images.unsplash.com',
      'images.pexels.com',
      'img.freepik.com',
      'cdn.pixabay.com'
    ],
  },
  /* config options here */
};

export default nextConfig;
